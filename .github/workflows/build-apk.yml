name: Build Android APK

on:
  push:
    branches:
      - beta
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # Must stay >= 3.27, which is the lowest stable bundling Dart 3.6 —
          # trireme_client v1.4.0 declares sdk: ">=3.6.0 <4.0.0" and a git
          # dependency resolves to exactly one version, so pub cannot fall back.
          flutter-version: '3.44.9'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      # Only errors fail the build. There is a standing backlog of ~218
      # info/warning findings, so bare `flutter analyze` would exit non-zero
      # on every run and the step would be worthless as a gate.
      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run tests
        run: flutter test

      - name: Build APK
        run: flutter build apk --release --flavor github

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v7
        with:
          name: trireme-release-apk
          path: build/app/outputs/flutter-apk/app-github-release.apk
